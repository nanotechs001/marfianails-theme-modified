{% section 'product-template' %}

<div class="text-slider-section">
  <div class="page-width">
    <div class="text-slider">
      <div class="text-slide">Free shipping on orders over $65 *Australia Wide Only*</div>
      <div class="text-slide">30% SITEWIDE CODE BEAUTY30 APPLIED AT CHECKOUT</div>
      <div class="text-slide">Nail now pay later with AFTERPAY, LATITUDE PAY & KLARNA</div>
    </div>
  </div>
</div>

{% section 'product-recommendations' %}

{% if collection %}
  <div class="text-center page-content page-content--bottom">
    <a href="{% if collection.handle == 'frontpage' %}/{% else %}{{ collection.url }}{% endif %}" class="btn btn--small return-link">
      <svg aria-hidden="true" focusable="false" role="presentation" class="icon icon--wide icon-arrow-left" viewBox="0 0 50 15"><path d="M50 5.38v4.25H15V15L0 7.5 15 0v5.38z"/></svg> {{ 'products.general.collection_return' | t: collection: collection.title }}
    </a>
  </div>
{% endif %}

<script>
  // Override default values of shop.strings for each template.
  // Alternate product templates can change values of
  // add to cart button, sold out, and unavailable states here.
  theme.productStrings = {
    addToCart: {{ 'products.product.add_to_cart' | t | json }},
    soldOut: {{ 'products.product.sold_out' | t | json }},
    unavailable: {{ 'products.product.unavailable' | t | json }}
  };

  document.addEventListener('DOMContentLoaded', function() {
    $('.text-slider').slick({
      arrows: false,
      dots: false,
      infinite: true,
      autoplay: true,
      autoplaySpeed: 5000,
      fade: true,
      cssEase: 'linear'
    });
  });
</script>

<style>
.product-divider {
  margin: 30px 0;
}

.product-divider .announcement-bar {
  border-top: 1px solid {{ settings.color_border }};
  border-bottom: 1px solid {{ settings.color_border }};
}

.text-slider-section {
  padding: 15px 0;
  margin: 30px 0;
  border-top: 1px solid {{ settings.color_border }};
  border-bottom: 1px solid {{ settings.color_border }};
  background: {{ settings.color_announcement }};
  color: {{ settings.color_announcement_text }};
}

.text-slider {
  text-align: center;
}

.text-slide {
  display: none;
  font-weight: bold;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  font-size: 0.85em;
}

.text-slide:first-child {
  display: block;
}
</style>

<div class="yotpo yotpo-main-widget" data-product-id="{{ product.id }}" data-name="{{ product.title | escape }}" data-url="{{ shop.url }}{{ product.url }}" data-image-url="{{ product.featured_image | product_img_url: 'large' |replace: '?', '%3F' | replace: '&amp;','%26'}}" data-description="{{ product.description | escape }}" data-price="{{ variant.price | money_without_currency }}" data-currency="{{ shop.currency }}"></div>

